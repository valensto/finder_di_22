export type UUID = string

export enum PersonRole {
    Student,
    Instructor,
    Pilot
}

export enum Extension {
    PDF,
    XML,
    JSON,
    TXT,
    DOC,
    DOCX,
    XLS,
    XLSX,
    PPT,
    PPTX,
    JPG,
    PNG,
    GIF
}